## Exercise 5.13

Use **unfold** to implement the following methods on LazyList as in we did for List in chapter 3

- `map`
- `take`
- `takeWhile`
- `zipWith`
- `zipAll`

The zipAll function should continue the traversal as long as either lazy list has more elements; it uses Option to indicate whether each lazy list has been exhausted:

```scala
def zipAll[B](that: LazyList[B]): LazyList[(Option[A], Option[B])]
```


## Solution

Look at datastructures/LazyList.scala

