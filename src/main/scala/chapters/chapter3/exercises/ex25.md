## Exercise 3.25

Write a function, `maximum`, that returns the maximum element in a `Tree[Int]`. (Note that in Scala you can use `x.max(y)` to compute the maximum of two integers `x` and `y`.)

## Solution

Look at datastrucutres/Tree.scala
