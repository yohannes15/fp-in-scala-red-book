## Exercise 7.13

Implement `chooser` and use it to implement `choice` and `choiceN`.

```scala
def chooser[B](choices: A => Par[B]): Par[B]
```

## Solution

Look at parallel/nonblocking/Par.scala

The key is realizing chooser is the same as flatMap and we already have flatMap. Also read the chapter readme notes.

