## Exercise 7.12

There’s still something rather arbitrary about `choiceN`: the choice of `List` seems overly specific. Why does it matter what sort of container we have? For instance, what if instead of a list of computations we have a `Map` of them?

```scala
def choiceMap[K, V](key: Par[K])(choices: Map[K, Par[V]]): Par[V]
```

## Solution

Look at parallel/nonblocking/Par.scala 
